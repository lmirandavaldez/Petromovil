-- Procedimiento para actualizar inactividad en fucturas compras

CREATE OR REPLACE PROCEDURE PRO_ACTUALIZAR_INACTIVIDAD(
    IN P_ID_CLIENTE DECIMAL(7,0),
    IN P_FECHA_COMPRA DATE
)
LANGUAGE SQL
BEGIN
    DECLARE V_FECHA_ULTIMA DATE;
    DECLARE V_MESES_INACTIVO DECIMAL(10,0);
    DECLARE V_EXISTE INT;
    DECLARE V_FECHA_INGRESO Date;

    -- Verificar si el cliente existe en CLIENTE_INACTIVIDAD
    SELECT COUNT(*) INTO V_EXISTE
    FROM CLIENTE_INACTIVIDAD
    WHERE ID_CLIENTE = P_ID_CLIENTE;
     -- Obtener la fecha de ingreso desde la tabla CXCCLI si el cliente es nuevo
     IF V_EXISTE = 0 THEN
         SELECT Date(CRETST) INTO V_FECHA_INGRESO
         FROM CXCCLI
         WHERE CliCve = P_ID_CLIENTE ;

         -- Insertar el cliente con su primera compra y fecha de ingreso
         INSERT INTO CLIENTE_INACTIVIDAD (ID_CLIENTE, FECHA_INGRESO,
 FECHA_PRIMERA_COMPRA, FECHA_ULTIMA_COMPRA,
                                          FECHA_ULTIMA_INACTIVIDAD,
 FECHA_REACTIVACION, MESES_INACTIVO, ES_NUEVO, ESTADO_CLIENTE)
         VALUES (P_ID_CLIENTE, V_FECHA_INGRESO, P_FECHA_COMPRA, P_FECHA_COMPRA,
 P_FECHA_COMPRA, P_FECHA_COMPRA, 0, 'S', 'ACTIVO');
     ELSE
         -- Obtener última compra
         SELECT FECHA_ULTIMA_COMPRA INTO V_FECHA_ULTIMA
         FROM CLIENTE_INACTIVIDAD
         WHERE ID_CLIENTE = P_ID_CLIENTE;

         -- Calcular meses de inactividad
         SET V_MESES_INACTIVO = MONTHS_BETWEEN(P_FECHA_COMPRA, V_FECHA_ULTIMA);

         -- Actualizar datos sin perder la última inactividad
         MERGE INTO CLIENTE_INACTIVIDAD AS CI
         USING (SELECT P_ID_CLIENTE, P_FECHA_COMPRA FROM SYSIBM.SYSDUMMY1) AS
 SRC
         ON CI.ID_CLIENTE = SRC.P_ID_CLIENTE
         WHEN MATCHED THEN
             UPDATE SET
                 FECHA_ULTIMA_INACTIVIDAD = CASE WHEN V_MESES_INACTIVO >= 36
 THEN CI.FECHA_ULTIMA_COMPRA ELSE CI.FECHA_ULTIMA_INACTIVIDAD END,
                 FECHA_ULTIMA_COMPRA = P_FECHA_COMPRA,
                 MESES_INACTIVO = V_MESES_INACTIVO,
                 ES_NUEVO = CASE WHEN V_MESES_INACTIVO >= 36 THEN 'S' ELSE 'N'
 END,
                 FECHA_REACTIVACION = CASE WHEN V_MESES_INACTIVO >= 36 THEN
 P_FECHA_COMPRA ELSE CI.FECHA_REACTIVACION END,
                 ESTADO_CLIENTE = 'ACTIVO';
     END IF;
 END;
